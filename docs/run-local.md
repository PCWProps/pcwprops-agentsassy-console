# run-local

## prerequisites
- Node runtime available via Volta (per tooling policy).

## start the console
1. From repo root: `node apps/agentsassy-console/server.js`
2. Open `http://localhost:3030`

## end-to-end request
- Use the UI form to submit a control request.
- The response should show `status: accepted` and a mock execution result.
